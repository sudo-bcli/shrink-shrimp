# file/folders listed below won't be tracked by git hence won't be uploaded to Github
# @author bcli

# node_modules, which should be re-installed depending on your platform
node_modules

# npm generated list of  node_modules
package-lock.json

# system files of OSX & Win
.DS_Store
Thumbs.db

# release builds generated by electron-packager
release-builds

# IDE configurations
# here we are only ignoring VSCode configurations, for more IDEs
# @see https://www.gitignore.io/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history